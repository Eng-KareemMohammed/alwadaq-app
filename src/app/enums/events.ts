export enum Events {
  refreshHome = 'refresh:Home',
  refreshOrders = 'refresh:Orders',
  refreshNews = 'refresh:News',
  refreshSubBrands = 'refresh:SubBrands',
  refreshUsers = 'refresh:Users',
  refreshDelegates = 'refresh:Delegates',
  refreshSliders = 'refresh:Sliders',
  refreshProducts = 'refresh:Products',
  refreshNotification = 'refresh:Notification',
}
